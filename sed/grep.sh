#!/usr/bin/env bash
# gerp is used for search string in file with regex pattern
#grep [-abcEFGhHilLnqrsvVwxy][-A<显示列数>][-B<显示列数>][-C<显示列数>][-d<进行动作>]
#[-e<范本样式>][-f<范本文件>][--help][范本样式][文件或目录...]

# list the line which include the BB string
grep BB test.txt
